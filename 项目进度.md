# KnowMate 项目进度

更新时间：2026-09-24

## 当前结论

项目已经具备本地开发所需的 GoFrame 后端、Python Agent、MCP 服务、记忆服务、MySQL、观测组件和 Web Admin。现在可以使用 `scripts/start-all.ps1` 一次提交开发服务启动；脚本不会执行数据库清空、迁移回滚或删除 Docker volume。

## 已完成

- GoFrame HTTP API、任务编排、MySQL 持久化和 Python gRPC 调用链已存在。
- Python Agent 的文章、反馈和聊天工作流已接入。
- Web Admin 支持文章、任务、反馈、聊天、推荐和记忆管理。
- 普通用户 `/wechat/chat` 页面和后端会话隔离 API 已实现，身份来自 HttpOnly Cookie，不信任前端提交的 `user_id` 或 `role_id`。
- 微信网页 OAuth 代码已实现：state 校验、`snsapi_base` 跳转、回调换 OpenID 和签名会话 Cookie。
- 局域网 HTTP 下发送消息的 `crypto.randomUUID` 兼容问题已修复。
- 环境模板已按用途整理，微信配置模板为 `configs/env/wechat.env.example`，生产模板使用 Secret 文件引用。
- 前端测试和构建已通过；聊天后端专项 Go 测试已通过。

## 当前开发方式

在项目根目录运行：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\start-all.ps1
```

可选参数：

- `-Build`：启动前先重新构建 Docker 镜像。
- `-NoWeb`：只启动 Docker 服务，不启动 Vite Web Admin。
- `-ProjectName <名称>`：使用指定的 Compose 项目名。

默认入口：

- Web Admin：`http://127.0.0.1:5173`
- GoFrame：`http://127.0.0.1:8080`
- 健康检查：`http://127.0.0.1:8080/health`

## 仍待完成

- 微信真实 OAuth 尚未完成公网 HTTPS、网页授权域名和真实账号端到端验收；本地 `WECHAT_DEV_ANONYMOUS=true` 仍是开发模式。
- 微信客服消息发送权限和真实服务号双向消息验收仍需账号侧条件。
- Mem_Pro 的 MongoDB、Neo4j、Milvus 和真实模型组合尚未完成生产级全链路验收。
- Web Admin 仍不是完整 RBAC 管理后台。
- 当前工作区仍有历史工作报告、测试输出、缓存和旧 Compose 文件等待确认的清理候选，未自动删除。

## 重要边界

- `docs/*_WORK_REPORT.md` 是历史验收证据，不代表当前服务一定正在运行。
- `.env`、`configs/env/prod.env` 和 `.local-server/<stage>/` 是本机运行配置，不应提交。
- 生产启动使用 `docker-compose.prod.yml` 和 `configs/env/prod.env`；开发启动使用 `docker-compose.yml` 和 `configs/env/dev.env`。
- 未完成公网、真实微信和真实模型验收前，不能把当前状态称为生产就绪。
